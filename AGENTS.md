# 작업 규칙

1. 모든 화면, 문서, 오류 메시지는 한국어로 작성한다.
2. 사용자가 요청한 단계의 기능만 구현하며, 다음 단계 기능을 임의로 선행 구현하지 않는다.
3. 원본 자료는 수정·삭제·이동하지 않는다. 데이터 파일에 행을 추가하지 않는다.
4. 모든 자료의 기본 연결 키는 `company_id`이며, 업체명 유사성만으로 자동 연결하지 않는다.
5. 연결이 불확실하면 담당자가 업체를 직접 선택하도록 한다.
6. 지도점검과 행정처분은 별도 데이터로 관리하며, 행정처분은 담당자 확인 없이 자동 등록·확정하지 않는다.
7. PDF 분석 결과도 담당자 확인 후에만 반영한다. 자료에 없는 내용은 생성하지 않는다.
8. 개인정보, 비밀번호, 실제 업무자료는 Git에 포함하지 않는다.
9. 기능 구현 뒤에는 직접 실행하고 가능한 범위의 테스트를 수행한다. 오류를 숨기지 말고 원인과 해결 내용을 보고한다.
10. 기존 정상 기능을 임의로 제거하지 않는다. 업무 규칙 또는 데이터의 의미가 불명확하면 사용자에게 질문한다.
11. 작업 후 변경 파일, 실행 명령, 테스트 결과, 잔여 문제를 보고한다.
12. Git commit, GitHub push, 외부 API 호출, Supabase 데이터 변경은 사용자의 명시적 요청이 있을 때만 수행한다.
